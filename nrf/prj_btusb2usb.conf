# Joypad btusb2usb — nRF52840 Kconfig overrides
#
# Applied on top of prj.conf when building APP_TYPE=btusb2usb.
# Combines USB host (MAX3421E) + Bluetooth. BT stays enabled from prj.conf.

# SPI driver disabled — MAX3421E uses direct NRF_SPI1 register access (ISR-safe)
CONFIG_SPI=n
